PORT=8787
DATA_DIR=/gremlin-data
# If API_KEY is empty and DISABLE_AUTH=false, the docker entrypoint will
# generate and persist one at DATA_DIR/auth.json.
API_KEY=
DISABLE_AUTH=false
ALLOWED_ORIGINS=*
